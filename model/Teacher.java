package model;
import java.util.List;

public class Teacher extends Person {
    public Teacher(String id, String name, String address, List<String> phones) {
        super(id, name, address, phones);
    }
}
