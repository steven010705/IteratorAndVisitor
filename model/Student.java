package model;
import java.util.List;

public class Student extends Person {
    public Student(String id, String name, String address, List<String> phones) {
        super(id, name, address, phones);
    }
}
